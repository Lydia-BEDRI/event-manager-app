PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

DB_HOST=localhost
DB_PORT=3307
DB_USER=eventmanager
DB_PASSWORD=eventmanager123
DB_NAME=eventmanager

JWT_SECRET=KeyForJWTTokenGeneration
JWT_EXPIRES_IN=15m
QR_CODE_PRIVATE_KEY=KeyForSignedQrCodeGeneration
QR_CODE_TOKEN_TTL=365d
TWO_FACTOR_ENCRYPTION_KEY=KeyForTwoFactorSecretEncryption

CORS_ORIGIN=http://localhost:3000

# Local development with Mailpit (docker-compose.local.yml)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=EventManager <no-reply@eventmanager.local>
